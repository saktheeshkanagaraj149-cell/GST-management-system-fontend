const handleLogout = () => {
  // Clear cookie
  document.cookie = 'token=; path=/; max-age=0';
  
  // Clear localStorage if used
  localStorage.removeItem('token');
  
  // Redirect to login
  router.push('/login');
};